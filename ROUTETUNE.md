# routetune

> 仓库首页在 [README.md](./README.md)。这份是 routetune 自己的完整文档。

[![CI](https://github.com/bear4f/routetune/actions/workflows/ci.yml/badge.svg)](https://github.com/bear4f/routetune/actions/workflows/ci.yml)
[![License: MIT](https://img.shields.io/badge/License-MIT-blue.svg)](./LICENSE)

按路由前缀观察 Linux TCP 客户端分布、累积画像，并生成可审阅、可回滚的 per-route 参数实验建议。

routetune 面向同一台服务器同时服务多地区固网、4G/5G 和同机房客户端的场景。第一阶段刻意保持保守：`scan`、`profiles`、`recommend`、`doctor` 都不改系统；只有你手动复制 `recommend` 输出的命令，路由表才会变化。

## 全局层：按观测到的群体定尺寸，不是按某一个客户端

单一 sysctl 在混合客户端上失效，不是因为它全局，而是因为它**总被按某一个客户端定尺寸**。
固定家宽调好的缓冲上限，对 250 ms 外的 5G 客户端就是一道查不出来的硬天花板。

routetune 观测过整个客户端群体，所以能按**最远那个**定尺寸：

```bash
routetune tune          # 预演：逐项列出当前值 → 目标值和依据，什么都不写
routetune tune --yes    # 真的写入，写前自动快照
routetune status        # 当前生效情况与目标值的差异
routetune revert        # 还原到 routetune 介入之前
```

尺寸取**单个前缀自己的** RTT × 它自己的**峰值**速率，在所有前缀里取最大——必须同一个前缀。
峰值而不是中位：一个前缀上 38 条连接里 37 条挂着不动、1 条在跑 400 Mbps 时，中位数接近 0，
按它定尺寸就是把那个真正有需求的客户端关进一道谁也解释不了的天花板。
取「所有前缀里最大的 RTT」乘「所有前缀里最大的速率」会得到一个不存在的工作点：
高 RTT 的链路正是慢的那条。（0.3.0 就是这么写的，一次高铁上的采样给出 975 ms / 9 Mbps，
配上 200 Mbps 下限后把机器定到了 46 MB。0.3.1 修掉了。）

未观测客户端另算一个兜底项：150 ms × 200 Mbps。两个下限**只作用在兜底项上**，不会去
放大一条慢链路的 RTT。最终 BDP = max(实测最大, 兜底)。下限的论证来自
[tcpfit](https://github.com/Kylin010/tcpfit)：代价不对称，估高只多付一点 BBR 超发，
估低是每个远端客户端都撞得到、却查不出原因的硬顶。

缓冲上限 = BDP × 2（`tcp_adv_win_scale=1` 时内核只把一半交给应用），再按内存夹住。

### 每个键只往安全的方向动

同一条不对称论证也意味着**不该收缩**：上限给大几乎不亏（autotuning 会让近端连接自己停在
低处），调小是查不出来的硬顶。所以每个键带一个方向：

| 方向 | 键 | 规则 |
|---|---|---|
| 只升不降 | `rmem_max` `wmem_max` `tcp_rmem` `tcp_wmem` | 当前值已经不低于目标就不动 |
| 只降不升 | `tcp_notsent_lowat` | 运维已经调得更紧就保持 |
| 精确 | `default_qdisc` `congestion_control` `slow_start_after_idle` `mtu_probing` | 就是要这个值 |

`tcp_rmem` / `tcp_wmem` 是 `min default max` 三元组，**逐字段合并**：中间那个是初始接收
窗口，抬高它让发送方在等窗口更新前多发一些（对爬升有用），而第三个上限如果已经更高就
保持——不会在同一次写入里升一个、缩另一个。

### 定位：体检，不接管

如果你已经在跑 netshape 之类的调优，`tune` 多半会告诉你**不需要 routetune 接管**，并说明
它是依据什么数据得出这个结论的。routetune 只在它能看到、而全局调优工具看不到的地方出声
——也就是**客户端分布**。

| 目标 | 靠什么 |
|---|---|
| **低重传** | `fq` pacing——没有 pacing 时一个突发按线速打出去，直接冲垮最慢那条末端链路的缓冲。这是混合客户端下最大的单一杠杆 |
| **爬升快** | 缓冲上限给足（避免硬顶）+ BBR + `tcp_slow_start_after_idle=0` + 干净远端路径的 `initcwnd 32` |
| **不掉速** | BBR 不会因随机丢包塌陷；不设会绑死的单流限速；流媒体分块之间不重置 cwnd |

`tcp_slow_start_after_idle=0` 值得单独说：默认行为是连接短暂空闲后把 cwnd 打回初始值
重新慢启动，而流媒体分块之间恰好就是这种空闲——**「看着看着掉速」很多时候就是这个**。

**与 netshape 不能共存**：两者都写全局 sysctl 和根队列，不是叠加而是谁后跑谁生效。
`tune` 检测到 netshape 会警告。

## 为什么不是再加一组 sysctl

sysctl 是全机共享的。一条丢 3% 的移动网、一条 250 ms 的稳定固网和一个 1 ms 的同机房客户端，不能分别拥有不同的全局 TCP 缓冲与拥塞控制。

Linux 路由可以携带 `initcwnd`、`initrwnd`、`rto_min`、`advmss`、`congctl` 等 TCP metrics。routetune 因而采用：

```text
ss 窗口采样 → 按 IPv4 /24、IPv6 /64 聚合 → 多轮画像 → per-route 实验建议
```

示例建议会复制内核当前为目标前缀选出的地址族、下一跳、网卡、源地址和路由表，只附加 TCP metrics：

```bash
sudo ip -4 route add 119.237.129.0/24 via 172.16.0.1 dev eth0 \
  src 192.0.2.10 table main initcwnd 32

# 对应回滚
sudo ip -4 route del 119.237.129.0/24 table main
```

安全边界：如果该前缀已有精确路由，或内核返回 ECMP/多路径，routetune 不生成命令。添加更具体的路由仍会固定当时的选路；默认路由以后发生变化时，它不会自动跟随，所以阶段一不持久化，并且每条建议都带精确回滚。

## 在 DMIT 上开始测试

依赖：Linux、Bash 4+、iproute2（`ss`、`ip`）；`doctor` 可选使用 `tc`、`nstat`。

```bash
curl -fsSL https://raw.githubusercontent.com/bear4f/routetune/main/routetune.sh \
  -o /tmp/routetune.sh
chmod +x /tmp/routetune.sh

# 纯只读快照；让 4G/5G 客户端保持真实视频流量
sudo bash /tmp/routetune.sh scan --samples 10 --interval 3

# 累积 30 分钟画像；写入 /var/lib/routetune/profiles.tsv，不改网络
sudo bash /tmp/routetune.sh watch --minutes 30 --samples 6 --interval 5
sudo bash /tmp/routetune.sh profiles
sudo bash /tmp/routetune.sh recommend --min-obs 5
sudo bash /tmp/routetune.sh doctor
```

`watch` 写入的画像库包含客户端前缀，权限为 `0600`。不再需要时可运行 `sudo bash /tmp/routetune.sh reset` 清空。

## 画像与当前策略

| 画像 | 判据 | 第一阶段输出 |
|---|---|---|
| 远端固网 | RTT ≥120 ms、膨胀 <1.5、且**已证实**低丢包（窗口增量 ≥1000 段） | A/B 测试 `initcwnd 32` |
| 近端固网 | RTT <120 ms 且稳定 | 不改；额外首窗突发通常没有收益 |
| 轻微排队 | 膨胀 1.5–3，无明显丢包 | 不改；路径已有存量队列时加大首窗只会把尖峰推高 |
| 丢包未证实 | 延迟形状可信（≥100 段），但未满 1000 段，重传率无从判断 | 不改；没证实低重传就不算干净固网 |
| 虚假重传 | 重传 ≥0.1%，且其中 ≥50% 被对端 DSACK 确认是重复包 | 不改；这条链路没在丢包，别按重传率降速 |
| 移动网络 | 重传 ≥1%，且抖动或延迟尾部散开 | 不改首窗；`initcwnd` 治不了长连接中的无线丢包和尾峰 |
| 排队且丢包 | 膨胀 ≥2 且重传 ≥1% | 不改；被动观测分不清队列和丢包出在路径的哪一段 |
| 稳定延迟丢包 | 重传 ≥1%，但延迟较平 | 不推断限速器；先用可控主动测速复测 |
| 轻度丢包 | 重传 0.1%–1% | 继续观察；不当作健康固网放大初始窗 |
| 接入网排队 | 中位 RTT / minRTT ≥3，且丢包低或未证实 | 不自动改；服务器无法直接管理对端队列 |
| 间歇排队 | 中位不深，但典型尾涨 ≥3，或 ≥10% 的轮次出现尾峰 | 延长观察 |
| 时变链路 | 抖动或尾部散布高，但没有明确丢包/排队形状 | 延长观察，可在测试机 A/B BBRv3 |
| 同机房 / 数据不足 | RTT <5 ms / 窗口增量 <100 段 | 不做判断 |

**只有「远端固网」会输出命令。** 它是唯一断言「路径干净」的类，所以三项证据必须同时
成立：没有存量队列、没有丢包、而且段数足够证实这一点。任何一项不成立都会落到别的类，
拿不到 `initcwnd 32`——给一条已经坐在底噪之上的路径加大首窗突发，方向是反的。

## 每个参数还要证明自己会生效

画像决定「这类链路适用什么参数」，但参数还要单独过一道 no-op 闸门才会出现在建议里。
一个证明不了会生效的参数不是保守建议，是噪音——它浪费你的时间，也消耗这个工具的可信度。

| 参数 | 只在什么时候提议 |
|---|---|
| `initcwnd` | 该前缀的发送时间没有大量卡在对端接收窗口上（否则首窗不是瓶颈） |
| `congctl` | 内核里真的存在比当前全局更合适的算法（`bbr2`/`bbr3`）。只有 `reno cubic bbr` 时不提议——把移动网段降级成 cubic 对随机无线丢包更差 |

被丢弃的参数会在 `recommend` 里列出原因，不会默默消失。

## 新增的证据（v0.2.0）

`ss -tin` 里这几项以前没取，现在按窗口增量统计并归因到前缀：

- **虚假重传占比** = `dsack_dups` 增量 ÷ 重传增量。`doctor` 里那个 DSACK 只是全机旁证，
  单位不同也无法归因；per-socket 的 `dsack_dups` 可以归因到具体前缀。同样是 6.6% 重传，
  DSACK 占 80% 和占 5% 是两条完全不同的链路。
- **受限原因**：`rwnd_limited` / `sndbuf_limited` 占 `busy` 的比例——直接回答「它为什么没更快」。
- **MSS 钳制**：协商出的 `mss` 明显低于 `pmtu` 允许的大小时提示（多半是隧道）。
- **上行占比**：`data_segs_in` 占比高说明该前缀主要在上传，本工具按下载场景推的策略未必适用。

这些是画像启发式，不是运营商识别。一个前缀被标为“移动网络”，含义是它在采样窗口里呈现无线调度常见的延迟与重传形状，不等于 routetune 查询到了该 IP 的运营商类型。

`profiles` 中的“峰95”是所有可信轮次里最高的一轮 RTT95，用来保留偶发卡顿尾峰；它不是把整个观测期所有 RTT 样本重新计算得到的全量 P95。

“峰95”只作为证据显示，**不参与分类**。最大值永远不会衰减：拿它去分类，意味着一次偶发尖峰会把一个前缀永久钉在告警类别里，而且观测越久越容易踩到一次——这与“轮次越多画像越可信”正好相反。分类看的是典型轮次（各轮均值）加上“尖峰”一列：有多少轮的 RTT95 涨到了底噪的 2 倍以上。361 轮里尖峰 1 次和尖峰 61 次是两种链路，最大值分不出来，频率分得出来。

升级到 0.1.4 时，旧画像库没有这些累计量。routetune 会用最大值 × 轮次来补种，也就是按“过去每一轮都是最差那轮”的悲观读法起步，再随新观测衰减回真实值——升级不会把一个坏画像悄悄变好。想要干净基线就先 `routetune reset`。

### 无线丢包与 policer 不能靠被动快照区分

两者都可能出现 2%–3% 重传，但处置方向不同：

- 无线调度/换手通常同时拉开 RTT 尾部；降一点服务端速率未必减少射频层丢包。
- policer 可能表现为重传突然升高、延迟形状仍较平，但移动路径也可能在一个短窗口里暂时呈现平稳延迟；只有在可控测速中找到可重复的速率拐点，才适合推导速率。

DMIT 真机上的同一个移动 `/24` 已经出现过连续两轮 RTT 形状近似、重传却从 3.07% 降到 0.51% 的情况。旧版先判“限速器”、再判“健康固网”，两次都过度推断。现在这两轮数据都锁进回归测试：前者只叫“稳定延迟丢包”，后者叫“轻度丢包”，两者都不生成路由修改。

## 四道数据质量闸门

1. 默认只显示本机监听端口上的入站连接，过滤服务器主动访问的 CDN、源站和更新连接；`scan --all` 可查看全部。
2. RTT <5 ms 时不根据 `rttvar / rtt` 告警，避免计时粒度和中断合并噪声。
3. 只使用采样窗口内 `data_segs_out` 与 retrans 的增量；绝不拿连接生命周期累计值填补空闲窗口。
4. 窗口增量不足 100 段时不判断路径；100–999 段只判断 RTT 形状并隐藏重传率；累计达到 1000 段后才让重传率参与分类。这样 991 段和 1000 段不会让整条路径在“健康”与“数据不足”之间跳变。

另有 `minrtt <0.1 ms` 的除零保护、IPv4-mapped 地址归一化，以及压缩/补零 IPv6 地址到规范 `/64` 的合并。

## 从 tcpfit 借鉴了什么

routetune 不复制 tcpfit 的全局调优模型，但采用了两个测量原则：

- RTT 是客户端分布，不应把一次 ping 当成整台服务器唯一的“地区 RTT”。routetune 直接观察真实连接，并按前缀保存分布。
- 低于预期的单次吞吐需要触发式补测并保留同一整组结果；因此 policer 画像当前只提示复测，不会拿一次 `delivery_rate` 快照直接生成限速。

如果后续加入主动 iperf3 验证，会使用 receiver goodput，并在首次结果低于基线阈值时才 best-of-N，避免正常链路无谓消耗流量。

## `doctor` 的解释边界

- 原厂主线内核只有名为 `bbr` 的主线实现时，脚本标为 BBRv1。非主线内核仍只显示 `bbr` 时，名称本身不足以判定 v1/v2/v3，需查内核构建说明。
- `TcpExtTCPDSACKRecv* / TcpRetransSegs` 只作为全机、开机以来的乱序/过早重传旁证。两个计数器单位不同，它不是“虚假重传百分比”，也不能归因到某个前缀。

## 做不到什么

- 无线射频层丢包不能由服务端 sysctl 或 per-route metrics 消除。
- BBRv3 需要内核提供；脚本不能把 BBRv1 变成 v3。
- 第一阶段不自动执行、不持久化路由、不生成 tc 速率。先用真实流量验证画像，再扩大动作面。
- NAT/CGNAT 后的一个前缀可能包含多个用户；前缀画像是聚合结果，不是单设备诊断。

## 测试

```bash
bash -n routetune.sh tests/self-test.sh
bash tests/self-test.sh
shellcheck -x -P SCRIPTDIR routetune.sh tests/self-test.sh
```

测试覆盖真机 4G/稳定延迟丢包/轻度丢包分类、时变无丢包链路、窗口增量、加权重传、IPv4-mapped 与 IPv6 `/64`、空闲连接闸门、路由上下文复制、ECMP/已有路由拒绝，以及 BBR/DSACK 解释。

其中一条是穷举 144 组参数、断言表格里的画像标签与它下面那行箭头诊断永远一致——这两者原本是各自独立的判定阶梯，0.1.5 之前有 29 组会互相矛盾。

## 项目状态

`v0.3.1` 起全局层可以落地（`tune --yes`，默认预演、写前快照、可 `revert`）。per-route 层仍然只出建议不自动执行——写错路由 metric 是事故，打印错了只浪费一分钟。

## 许可证

[MIT](./LICENSE)
